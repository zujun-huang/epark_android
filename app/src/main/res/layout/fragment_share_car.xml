<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <cn.epark.view.StatusBarHeightView
        android:id="@+id/status_bar"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="@color/theme_color"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <android.support.design.widget.TabLayout
        android:id="@+id/share_park_tab"
        app:tabTextAppearance="@style/TabLayoutTxtSize"
        android:layout_width="0dp"
        android:layout_height="50dp"
        android:background="@color/theme_color"
        app:tabIndicatorColor="@color/colorWhite"
        app:tabGravity="fill"
        app:tabMode="fixed"
        app:tabBackground="@color/transparent"
        app:tabRippleColor="@color/transparent"
        app:tabSelectedTextColor="@color/colorWhite"
        app:tabTextColor="@color/c7Fffffff"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/status_bar" />

    <android.support.v4.view.ViewPager
        android:id="@+id/share_park_vp"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:scrollbars="none"
        android:overScrollMode="never"
        android:background="@color/gEEF2F5"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/share_park_tab" />

</android.support.constraint.ConstraintLayout>